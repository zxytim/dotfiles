#!/bin/bash -e
set -x

BASE_DIR="$HOME/Desktop"
mkdir -p "$BASE_DIR"

get_last_screeshot() {
    echo "$BASE_DIR/$(ls -t "$BASE_DIR" | grep -v edit | grep DeepinScreenshot | head -n 1)"
}

deepin-screenshot -n 
xclip -selection clipboard -t image/png -i "$(get_last_screeshot)"
